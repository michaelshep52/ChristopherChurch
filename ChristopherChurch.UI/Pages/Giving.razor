@page "/Giving"
<!--
<form @onsubmit="MakePayment">
    <label for="cardElement">
        Credit or debit card
    </label>
    <div id="cardElement">
    </div>

    <div id="card-errors" role="alert"></div>

    <button type="submit">Submit Payment</button>
</form>
-->

@code {
            private async Task MakePayment()
            {
                StripeConfiguration.ApiKey = "your_secret_key_here"; // Replace with your actual secret key

                var options = new PaymentIntentCreateOptions
                {
                    Amount = 1000, // Replace with the amount in cents
                    Currency = "usd",
                    Description = "Test Payment",
                    Confirm = true,
                };
            }
        }

      <!-- var service = new PaymentIntentService();
        var intent = service.Create(options);
        var paymentIntent = await Stripe.StripeNet.StripeUtility.InitializeStripeClient()
            .ConfirmPaymentIntentAsync(intent.Id);

        if (paymentIntent.Status == "succeeded")
        {
            // Payment successful, you can perform additional actions here
            Console.WriteLine("Payment successful!");
        }
        else
    {
        // Payment failed, handle the error
        Console.WriteLine("Payment failed. Error: " + paymentIntent.LastPaymentError?.Message);
    }
    }
}-->


